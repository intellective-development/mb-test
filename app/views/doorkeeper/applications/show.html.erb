<div class="page-header">
  <h1>Application: <%= @application.name %></h1>
</div>

<div class="row">
  <div class="col-md-8">
    <h4>Application Id:</h4>

    <p><code id="application_id"><%= @application.uid %></code></p>

    <h4>Secret:</h4>

    <p><code id="secret"><%= @application.secret %></code></p>

    <h4>Callback urls:</h4>

    <table>
      <% @application.redirect_uri.split.each do |uri| %>
        <tr>
          <td>
            <code><%= uri %></code>
          </td>
          <td>
            <%= link_to 'Authorize', oauth_authorization_path(client_id: @application.uid, redirect_uri: uri, response_type: 'code'), class: 'btn btn-success', target: '_blank' %>
          </td>
        </tr>
      <% end %>
    </table>

    <h4>Capture default shipping address and payment method during authorization grant flow:</h4>
    <p><code id="capture_defaults_on_authorization"><%= @application.capture_defaults_on_authorization %></code></p>

    <h4>Capture payment method during authorization grant flow:</h4>
    <p><code id="capture_payment_method_on_authorization"><%= @application.capture_payment_method_on_authorization %></code></p>

    <h4>Allow clients to finalize orders (vs. faking it)</h4>
    <p><code id="allow_order_finalization"><%= @application.allow_order_finalization %></code></p>

    <h4>Skip authentication fraud check and verification email:</h4>
    <p><code id="skip_account_take_over_check"><%= @application.skip_account_take_over_check %></code></p>
  </div>

  <div class="col-md-4">
    <h3>Actions</h3>

    <p><%= link_to 'Edit', edit_oauth_application_path(@application), class: 'btn btn-primary' %></p>

    <p><%= render 'delete_form', application: @application, submit_btn_css: 'btn btn-danger' %></p>
  </div>
</div>
