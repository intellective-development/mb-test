<h1>New Promotional Landing Page</h1>
<%= form_for(@page, url: admin_config_promo_pages_path) do |f| %>
  <%= render partial: 'form', locals: { f: f } %>
  <%= f.submit "Create", class: "button" %>
<% end %>
<%= link_to 'Back', admin_config_promo_pages_path, class: 'button' %>
