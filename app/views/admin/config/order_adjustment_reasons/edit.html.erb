<h3>Editing Adjustment Reason:  <%= @reason.name %> </h3>

<%= form_for(@reason, :url => admin_config_order_adjustment_reason_path(@reason)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>

<%= link_to 'Back', admin_config_order_adjustment_reasons_path, :class => 'button' %>
