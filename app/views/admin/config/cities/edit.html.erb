<h3>Editing City:  <%= @city.name %> </h3>

<%= form_for(@city, url: admin_config_city_path(@city)) do |f| %>
  <%= render partial: 'form', locals: { f: f} %>
  <%= f.submit "Update", class: "button" %>
<% end %>

<%= link_to 'Back', admin_config_region_pages_path, :class => 'button' %>
