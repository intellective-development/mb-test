<% if @tax_category.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@tax_category.errors.count, "error") %> prohibited this from being saved:</h2>

    <ul>
    <% @tax_category.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<fieldset>
  <%= f.input :name %>
  <%= f.input :purpose, collection: TaxCategory.purposes.keys, selected: 'product', allow_blank: false %>
  <%= f.input :description %>
</fieldset>

<%= f.submit "Create", :class => "button" %>