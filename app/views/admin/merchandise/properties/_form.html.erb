<fieldset id="admin-property-names">
  <label>Identifing Name</label>
  <%= form.text_field :identifing_name %>

  <label>Display Name</label>
  <%= form.text_field :display_name %>

<% Prototype.find_each do |prototype| %>
  <div class="property_form left span-9 last">
    <% checked = prototype.name && prototype.properties.include?(@property)%>
    <%- form_name = "property[prototype_ids][]" %>
    <%= check_box_tag form_name, prototype.id, checked %>
    <%= prototype.name %>
  </div>
<% end %>

</fieldset>