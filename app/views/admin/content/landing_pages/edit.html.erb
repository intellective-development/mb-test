<h3>Editing Page:  <%= @landing_page.permalink %> </h3>

<%= form_for(@landing_page, url: admin_content_landing_page_path) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
  <%= f.submit "Update", class: "button" %>
<% end %>

<%= link_to 'Back', admin_content_landing_pages_path, :class => 'button' %>
