<h3>New Promotion</h3>
<%= simple_form_for(@promotion, :as => :promotion, :url => admin_generic_promotions_path(@promotion)) do |f| %>
  <%= render :partial => 'form', :locals => {:f => f} %>
<% end %>
<%= link_to "Back to List", admin_generic_promotions_path, :class => "button" %>