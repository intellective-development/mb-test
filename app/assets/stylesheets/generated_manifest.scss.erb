// We import the css generated by webpack here.
// In DEV, the generated styles are served by webpack, so we don't include it here to avoid duplicating it

// NOTE: when switching ENV_NAME_CLIENT between development and production,
// rails may continue to serve a cached asset with the generated styles.
// You can bust the cache by deleting and re-adding this if statement
<% if ENV['ENV_NAME_CLIENT'] != 'development' %>
@import "generated/shared_bundle"
<% end %>